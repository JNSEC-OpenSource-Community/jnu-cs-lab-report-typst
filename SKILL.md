---
name: jnu-cs-lab-report-typst
description: 使用 Typst 创建、填写、修改或导出江南大学人工智能与计算机学院实验报告，并生成版式一致的 PDF 和可编辑 DOCX。适用于用户提到江南大学计算机学院实验报告、实验报告模板、report.typ，或要求把实验材料整理为该院格式时。
---

# 江南大学计算机学院实验报告

使用本 skill 所在目录中的模板创建实验报告。用户的明确要求优先于以下默认规则。

## 创建或编辑报告

1. 将 `SKILL.md` 所在目录视为模板根目录。创建新报告项目时，将以下文件复制到用户指定的目标目录，并保持相对路径不变：
   - `report.typ`
   - `Makefile`
   - `scripts/typ2docx-safe.py`
2. 不要把生成的报告或用户资料写回已安装的 skill 目录，除非用户明确要求直接修改该目录中的模板。
3. 若目标目录已经有报告项目，直接编辑现有文件，不要覆盖用户的其他内容。
4. 优先只修改 `report.typ` 中“可编辑内容”区域的字段：
   - 表头：`course_name`、`experiment_name`、`experiment_date`、`class_name`、`student_name`、`student_id`、`instrument_id`、`report_requirements`
   - 正文：`experiment_purpose`、`experiment_content`、`program_list`、`run_status`、`reflection`
5. 除非用户要求调整版式，否则保留“版式定义”区域、页面尺寸、字体、表格尺寸和教师评价区。

## 整理内容

- 忠实使用用户给出的课程信息、实验要求、源代码、运行结果和心得；不要杜撰姓名、学号、实验数据或运行截图。
- 信息缺失但不影响继续工作时，使用简短、醒目的待补充文本，并在交付时列出缺项。若用户只要求空白模板，则清空示例身份和示例正文。
- 将程序代码放入 `program_list` 的 Typst 原始代码块中，并填写正确的语言标识；保留代码本身的缩进和换行。
- 将用户提供的图片复制到报告项目的 `assets/` 目录，使用相对于项目根目录的路径引用。不要留下指向不存在文件的 `#image(...)`。
- 实验内容较长时可使用 Typst 的列表、表格、公式、图片和小标题，但保持现有五个一级栏目及其顺序。

## 导出与检查

1. 始终先保证 `report.typ` 可以编译。
2. 用户未指定输出格式时，运行 `make all`，交付 `report.typ`、`report.pdf` 和 `report.docx`；只要求某一种格式时运行 `make pdf` 或 `make docx`。
3. DOCX 导出通过 `uv` 临时准备 `typ2docx`、`pygments` 等依赖。首次运行需要下载依赖时，遵守当前环境的联网和授权要求。
4. 检查构建命令的退出状态，并检查最终文件确实存在且非空。
5. 对最终 PDF 做视觉检查，重点确认分页、正文是否溢出、中文字体、表格边框、代码换行和图片位置。若同时生成 DOCX，也检查表头字段仍可编辑且没有明显错位。
6. 若缺少 Typst、uv、中文字体或转换依赖，说明具体缺项，并保留已经完成且可交付的源文件或输出文件。

## 交付

简要说明生成或修改了哪些文件、执行了哪些构建检查，以及仍需用户补充的内容。不要声称未实际运行的代码或实验已经验证通过。
